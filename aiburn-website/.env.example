# =============================================================================
# SECURITY NOTE: Do NOT commit actual values to this file
# All secrets should be stored in:
#   - Vercel Environment Secrets (production/preview)
#   - GitHub Secrets (for CI/CD)
#   - Local .env.local (never committed)
# =============================================================================

# Server Configuration
NODE_ENV=production
VITE_API_URL=https://aiburn.howstud.io/api

# Error Tracking (Sentry)
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# Application Version (auto-set in CI/CD)
VITE_APP_VERSION=1.0.0

# Email Configuration for Contact Form
# ⚠️ NEVER commit actual email credentials
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-specific-password
SMTP_FROM=noreply@aiburn.dev

# Email Recipients
CONTACT_EMAIL=tryaiburn@howstud.io
ADVERTISE_EMAIL=aiburnads@howstud.io

# Third-Party API Keys
# ⚠️ These should be stored in Vercel Secrets and NOT in .env files
OPENAI_API_KEY=sk-...  # For backend usage only
ANTHROPIC_API_KEY=sk-ant-...  # For backend usage only

# Database Configuration (if applicable)
DATABASE_URL=postgresql://user:password@localhost/aiburn

# CSP Report Endpoint (optional)
CSP_REPORT_URI=https://your-domain.com/api/csp-report

# Feature Flags
ENABLE_EXACT_MODE=true
ENABLE_ANALYTICS=false
ENABLE_BETA_FEATURES=false

# Production Configuration
DOMAIN=aiburn.howstud.io
PROTOCOL=https
PORT=443

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100

# Data Retention (days)
EMAIL_RETENTION_DAYS=90
ERROR_LOG_RETENTION_DAYS=30
ANALYTICS_RETENTION_DAYS=730
